const defaultState = {
    categorie: null,
    sortBy: {
        name: 'популярности',
        type: 'rating',
        order: 'desc'
    }
};

const filter = (state = defaultState, action) => {

};

export default filter;